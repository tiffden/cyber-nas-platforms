# Copy to .env and customize as needed.
# The launcher script (Scripts/run-local.sh) loads this file automatically.
#
# Current team convention:
# - install SPKI tools into ~/bin via `make install`
# - point UI bridge vars to those binaries
#
# Future TODO:
# - add bridge-side auto-discovery preference for ~/bin before explicit env vars.

# Key directory for keyring screen and key operations
SPKI_KEY_DIR=$HOME/.spki/keys

# CLI tool locations (recommended default for now)
SPKI_STATUS_BIN=$HOME/bin/spki-status
SPKI_SHOW_BIN=$HOME/bin/spki-show
SPKI_KEYGEN_BIN=$HOME/bin/spki-keygen
SPKI_REALM_BIN=$HOME/bin/spki-realm
SPKI_AUDIT_BIN=$HOME/bin/spki-audit
SPKI_VAULT_BIN=$HOME/bin/spki-vault
SPKI_CERTS_BIN=$HOME/bin/spki-certs
SPKI_AUTHZ_BIN=$HOME/bin/spki-authz

# Optional alternative: use local build artifacts directly
# SPKI_STATUS_BIN=/absolute/path/to/spki/_build/default/bin/spki_status.exe
# SPKI_SHOW_BIN=/absolute/path/to/spki/_build/default/bin/spki_show.exe
# SPKI_KEYGEN_BIN=/absolute/path/to/spki/_build/default/bin/spki_keygen.exe
# SPKI_REALM_BIN=/absolute/path/to/spki/_build/default/bin/spki_realm.exe
# SPKI_AUDIT_BIN=/absolute/path/to/spki/_build/default/bin/spki_audit.exe
# SPKI_VAULT_BIN=/absolute/path/to/spki/_build/default/bin/spki_vault.exe
# SPKI_CERTS_BIN=/absolute/path/to/spki/_build/default/bin/spki_certs.exe
# SPKI_AUTHZ_BIN=/absolute/path/to/spki/_build/default/bin/spki_authz.exe

# Realm backend (Chez Scheme) integration
# Needed by spki-realm to call real (cyberspace auto-enroll) functions.
# If you use `make ui-run`, launcher defaults these automatically.
# SPKI_CHEZ_BIN=chez
# SPKI_CHEZ_SCRIPT=/absolute/path/to/spki/scheme/chez/spki-realm.sps
# SPKI_CHEZ_STATUS_SCRIPT=/absolute/path/to/spki/scheme/chez/spki-status.sps
# SPKI_CHEZ_SHOW_SCRIPT=/absolute/path/to/spki/scheme/chez/spki-show-bridge.sps
# SPKI_CHEZ_KEYGEN_SCRIPT=/absolute/path/to/spki/scheme/chez/spki-keygen.sps
# SPKI_CHEZ_CERTS_SCRIPT=/absolute/path/to/spki/scheme/chez/spki-certs.sps
# SPKI_CHEZ_AUTHZ_SCRIPT=/absolute/path/to/spki/scheme/chez/spki-authz.sps
# SPKI_CHEZ_AUDIT_SCRIPT=/absolute/path/to/spki/scheme/chez/spki-audit.sps
# SPKI_CHEZ_VAULT_SCRIPT=/absolute/path/to/spki/scheme/chez/spki-vault.sps
# SPKI_CHEZ_LIBDIR=/absolute/path/to/spki/scheme/chez
# SPKI_REALM_WORKDIR=/absolute/path/to/spki/scheme/chez
