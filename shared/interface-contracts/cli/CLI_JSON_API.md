# SPKI CLI JSON API Contract (Mirror)

Canonical source of truth:

- `../cyber-nas-overlay/spki/docs/CLI_JSON_API.md`

This file is a compatibility mirror placeholder in `cyber-nas-platforms`.

Rules:

- Make all contract changes in the overlay repo first.
- If UI/client code in this repo needs the latest contract text, sync from overlay.
- Do not treat this file as authoritative.
